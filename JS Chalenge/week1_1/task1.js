// Task 1
// Write an "if statement" that checks whether the variable first
//  is bigger than the variable second. If true, log "first is bigger than second" to the console.

const first = 5
const second = 4

if ( first > second ) {
    console.log("first is bigger than second");
}