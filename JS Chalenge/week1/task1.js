// Create an if statement that checks if the variable temperature 
// is greater than the variable limit. If it is, 
// print: "temperature is higher than limit"

const temperature = 30;
const limit = 25;

if (temperature > limit) {
    console.log('temperature is higher than limit')
}